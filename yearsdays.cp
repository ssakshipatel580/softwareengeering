#include<stdio.h>
int main()
{
	int ndays,year,days;
	printf("enter the year");
	scanf("%d",&ndays);
	year=ndays/365;
	days=ndays/365;
	printf("%d is equivalent to %d years, % daysn % days", ndays ,year , days);
	return 0;
}
